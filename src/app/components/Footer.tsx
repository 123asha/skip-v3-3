// Social icons live in the bottom-right circles (rendered in App.tsx).
// This component is intentionally empty — kept as a no-op so existing imports don't break.
export default function Footer() {
  return null;
}
